import {
    Controller,
    Get,
    Post,
    Put,
    Delete,
    Body,
    Param,
} from '@nestjs/common';

@Controller('forms')
export class FormsController {

    // @Get()
    // findAll(): Promise<Item[]> {
    //   return this.itemsService.findAll();
    // }
}
